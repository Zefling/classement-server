meta {
  name: /api/admin/user/{id}_1
  type: http
  seq: 14
}

delete {
  url: {{base_url}}/api/admin/user/{{userId}}
  body: none
  auth: inherit
}

headers {
  X-AUTH-TOKEN: {{auth_token}}
}

vars:pre-request {
  userId: 23
}

settings {
  encodeUrl: true
}
