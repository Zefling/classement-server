meta {
  name: /api/admin/stats
  type: http
  seq: 15
}

get {
  url: {{base_url}}/api/admin/stats?=
  body: json
  auth: inherit
}

params:query {
  : 
}

headers {
  X-AUTH-TOKEN: {{auth_token}}
}

settings {
  encodeUrl: true
}
