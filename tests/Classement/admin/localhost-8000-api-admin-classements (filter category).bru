meta {
  name: /api/admin/classements (filter category)
  type: http
  seq: 4
}

get {
  url: {{base_url}}/api/admin/classements?page=1&category=anime
  body: json
  auth: inherit
}

params:query {
  page: 1
  category: anime
}

headers {
  X-AUTH-TOKEN: {{auth_token}}
}

settings {
  encodeUrl: true
}
