meta {
  name: /api/admin/classements (sort name)
  type: http
  seq: 6
}

get {
  url: {{base_url}}/api/admin/classements?order=name&direction=ASC 
  body: json
  auth: inherit
}

params:query {
  order: name
  direction: ASC 
}

headers {
  X-AUTH-TOKEN: {{auth_token}}
}

settings {
  encodeUrl: true
}
