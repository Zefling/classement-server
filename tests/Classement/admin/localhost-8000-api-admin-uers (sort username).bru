meta {
  name: /api/admin/uers (sort username)
  type: http
  seq: 11
}

get {
  url: {{base_url}}/api/admin/users?order=username&direction=ASC
  body: json
  auth: inherit
}

params:query {
  order: username
  direction: ASC
}

headers {
  X-AUTH-TOKEN: {{auth_token}}
}

settings {
  encodeUrl: true
}
