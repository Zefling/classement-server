meta {
  name: /api/admin/classements (page)
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/admin/classements?page=1
  body: json
  auth: inherit
}

params:query {
  page: 1
}

headers {
  X-AUTH-TOKEN: {{auth_token}}
}

settings {
  encodeUrl: true
}
