meta {
  name: /api/admin/classement/status/{id} (category)
  type: http
  seq: 9
}

post {
  url: {{base_url}}/api/admin/classement/status/{{classementId}}
  body: json
  auth: inherit
}

headers {
  X-AUTH-TOKEN: {{auth_token}}
}

body:json {
  {
      "status" : "food",
      "type" : "category"
  }
}

vars:pre-request {
  classementId: fe8e7bec853ecbe342cebef68dce1c15a57d27c7
}

settings {
  encodeUrl: true
}
