meta {
  name: /api/admin/uers
  type: http
  seq: 10
}

get {
  url: {{base_url}}/api/admin/users
  body: json
  auth: inherit
}

headers {
  X-AUTH-TOKEN: {{auth_token}}
}

settings {
  encodeUrl: true
}
