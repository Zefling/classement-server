meta {
  name: /api/admin/uers (filter username)
  type: http
  seq: 12
}

get {
  url: {{base_url}}/api/admin/users?username=zef
  body: json
  auth: inherit
}

params:query {
  username: zef
}

headers {
  X-AUTH-TOKEN: {{auth_token}}
}

settings {
  encodeUrl: true
}
