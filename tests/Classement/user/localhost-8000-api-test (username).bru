meta {
  name: /api/test (username)
  type: http
  seq: 16
}

post {
  url: {{base_url}}/api/test
  body: json
  auth: inherit
}

body:json {
  {
      "username": "dunglasx"
  }
}

settings {
  encodeUrl: true
}
