meta {
  name: /api/test (email)
  type: http
  seq: 17
}

post {
  url: {{base_url}}/api/test
  body: json
  auth: inherit
}

body:json {
  {
      "email": "dunglas@example.com"
  }
}

settings {
  encodeUrl: true
}
