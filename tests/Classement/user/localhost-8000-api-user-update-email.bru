meta {
  name: /api/user/update/email
  type: http
  seq: 8
}

post {
  url: {{base_url}}/api/user/update/email
  body: json
  auth: inherit
}

headers {
  X-AUTH-TOKEN: {{auth_token}}
}

body:json {
  {
      "emailOld": "dunglas@example10b.com",
      "emailNew": "dunglas@example10c.com"
  }
}

settings {
  encodeUrl: true
}
