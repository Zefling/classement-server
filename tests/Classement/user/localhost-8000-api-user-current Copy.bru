meta {
  name: /api/user/current Copy
  type: http
  seq: 14
}

get {
  url: {{base_url}}/api/user/current
  body: json
  auth: inherit
}

headers {
  ~X-AUTH-TOKEN: o5yadg6cb8gk88ogswskw8w48c0w444
}

docs {
  # Get Current User (Copy)
  
  Retrieves information about the currently authenticated user. This is a duplicate test case.
  
  ## Authentication
  Authentication token is disabled in this test (header is commented out with ~).
  
  ## Response
  Returns the current user's profile information including username, email, and preferences.
  
  ## Notes
  - This appears to be a test copy for unauthenticated access testing
  - The authentication header is intentionally disabled
}

assert {
  res.status: in [200, 401]
}

settings {
  encodeUrl: true
}
