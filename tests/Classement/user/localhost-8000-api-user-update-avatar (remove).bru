meta {
  name: /api/user/update/avatar (remove)
  type: http
  seq: 11
}

post {
  url: {{base_url}}/api/user/update/avatar
  body: json
  auth: inherit
}

headers {
  X-AUTH-TOKEN: {{auth_token}}
}

body:json {
  {
      "avatar": ""
  }
}

settings {
  encodeUrl: true
}
