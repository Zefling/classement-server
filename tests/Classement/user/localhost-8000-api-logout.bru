meta {
  name: /api/logout
  type: http
  seq: 18
}

delete {
  url: {{base_url}}/api/logout
  body: none
  auth: inherit
}

headers {
  X-AUTH-TOKEN: {{auth_token}}
}

settings {
  encodeUrl: true
}
