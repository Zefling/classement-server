meta {
  name: /api/user/current
  type: http
  seq: 13
}

get {
  url: {{base_url}}/api/user/current
  body: json
  auth: inherit
}

headers {
  X-AUTH-TOKEN: {{auth_token}}
}

settings {
  encodeUrl: true
}
