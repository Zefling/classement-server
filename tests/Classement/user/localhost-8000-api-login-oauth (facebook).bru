meta {
  name: /api/login/oauth (facebook)
  type: http
  seq: 7
}

post {
  url: {{base_url}}/api/login/oauth
  body: formUrlEncoded
  auth: inherit
}

headers {
  ~Language: fr
}

settings {
  encodeUrl: true
}
