meta {
  name: /api/classements (tag)
  type: http
  seq: 20
}

get {
  url: {{base_url}}/api/classements?mode=default
  body: none
  auth: inherit
}

params:query {
  mode: default
}

settings {
  encodeUrl: true
  timeout: 0
}
