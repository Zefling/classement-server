meta {
  name: /api/themes (user)
  type: http
  seq: 7
}

get {
  url: {{base_url}}/api/themes?user={{userId}}
  body: none
  auth: inherit
}

params:query {
  user: {{userId}}
}

vars:pre-request {
  userId: 31
}

settings {
  encodeUrl: true
}
