meta {
  name: /api/themes (name)
  type: http
  seq: 6
}

get {
  url: {{base_url}}/api/themes?name={{name}}
  body: none
  auth: inherit
}

params:query {
  name: {{name}}
}

vars:pre-request {
  name: titre
}

settings {
  encodeUrl: true
}
