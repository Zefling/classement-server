meta {
  name: /api/themes (name)
  type: http
  seq: 6
}

get {
  url: {{base_url}}/api/themes?name=titre
  body: none
  auth: inherit
}

params:query {
  name: titre
}

settings {
  encodeUrl: true
}
