meta {
  name: /api/themes (mode)
  type: http
  seq: 5
}

get {
  url: {{base_url}}/api/themes?mode={{mode}}
  body: none
  auth: inherit
}

params:query {
  mode: {{mode}}
}

vars:pre-request {
  mode: default
}

settings {
  encodeUrl: true
}
