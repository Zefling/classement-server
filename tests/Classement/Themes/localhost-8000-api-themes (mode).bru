meta {
  name: /api/themes (mode)
  type: http
  seq: 5
}

get {
  url: {{base_url}}/api/themes?mode=default
  body: none
  auth: inherit
}

params:query {
  mode: default
}

settings {
  encodeUrl: true
}
